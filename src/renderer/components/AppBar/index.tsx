import * as React from 'react';

import PathView from '../PathView';
import { StyledAppBar } from './styles';

export default () => {
  return (
    <StyledAppBar>
      <PathView />
    </StyledAppBar>
  );
};
