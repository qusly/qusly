import * as React from 'react';

import Tabbar from '../Tabbar';
import { StyledAppbar } from './styles';

export default () => {
  return (
    <StyledAppbar>
      <Tabbar />
    </StyledAppbar>
  );
};
