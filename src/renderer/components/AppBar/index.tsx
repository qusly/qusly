import * as React from 'react';

import { PathView } from '../PathView';
import { StyledAppBar } from './styles';

export const AppBar = () => (
  <StyledAppBar>
    <PathView />
  </StyledAppBar>
);
