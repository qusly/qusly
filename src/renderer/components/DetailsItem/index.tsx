import * as React from 'react';

import { StyledItem, Property, Value } from './styles';

export default () => {
  return (
    <StyledItem>
      <Property>Property</Property>
      <Value>Value</Value>
    </StyledItem>
  );
};
